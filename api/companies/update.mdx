---
title: "Update Company"
api: "PUT /companies/{id}"
description: "Update an existing company's information"
---

## Authentication

This endpoint requires authentication via Bearer token.

## Authorization

Required permission: `company:edit`

## Path Parameters

<ParamField path="id" type="number" required>
  The unique identifier of the company to update
</ParamField>

## Request Body

All fields are optional. Only include fields that need to be updated.

<ParamField body="nombre" type="string" optional>
  Company name (max 200 characters)
</ParamField>

<ParamField body="nombreLegal" type="string" optional>
  Legal company name (max 300 characters)
</ParamField>

<ParamField body="cedula" type="string" optional>
  Company tax ID/cedula (max 50 characters). Must be unique across all companies.
</ParamField>

<ParamField body="prefijo" type="string" optional>
  Company prefix (1-10 characters). Must be unique across all companies.
</ParamField>

<ParamField body="actividadEconomica" type="string" optional>
  Economic activity description (max 300 characters)
</ParamField>

<ParamField body="idExterno" type="string" optional>
  External system ID (max 100 characters). Must be unique if provided.
</ParamField>

<ParamField body="direccionExacta" type="string" optional>
  Exact physical address
</ParamField>

<ParamField body="telefono" type="string" optional>
  Phone number (max 30 characters)
</ParamField>

<ParamField body="email" type="string" optional>
  Company email address (max 150 characters). Must be a valid email format.
</ParamField>

<ParamField body="codigoPostal" type="string" optional>
  Postal code (max 20 characters)
</ParamField>

## Response

Returns the updated company object.

<ResponseField name="id" type="number">
  Unique identifier for the company
</ResponseField>

<ResponseField name="nombre" type="string">
  Company name
</ResponseField>

<ResponseField name="nombreLegal" type="string">
  Legal company name
</ResponseField>

<ResponseField name="cedula" type="string">
  Company tax ID/cedula
</ResponseField>

<ResponseField name="actividadEconomica" type="string" optional>
  Economic activity description
</ResponseField>

<ResponseField name="prefijo" type="string">
  Company prefix
</ResponseField>

<ResponseField name="idExterno" type="string" optional>
  External system ID
</ResponseField>

<ResponseField name="direccionExacta" type="string" optional>
  Exact physical address
</ResponseField>

<ResponseField name="telefono" type="string" optional>
  Phone number
</ResponseField>

<ResponseField name="email" type="string" optional>
  Company email address
</ResponseField>

<ResponseField name="codigoPostal" type="string" optional>
  Postal code
</ResponseField>

<ResponseField name="estado" type="number">
  Company state (1 = active, 0 = inactive)
</ResponseField>

<ResponseField name="fechaCreacion" type="string">
  Creation timestamp (ISO 8601 format)
</ResponseField>

<ResponseField name="fechaModificacion" type="string">
  Last modification timestamp (ISO 8601 format)
</ResponseField>

<ResponseField name="fechaInactivacion" type="string" optional>
  Inactivation timestamp (ISO 8601 format)
</ResponseField>

<ResponseField name="creadoPor" type="number">
  User ID who created the company
</ResponseField>

<ResponseField name="modificadoPor" type="number">
  User ID who last modified the company
</ResponseField>

<RequestExample>
```bash cURL
curl -X PUT "https://api.newkipital.com/companies/1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "nombre": "Acme Corporation Ltd",
    "telefono": "+506 2222-4444",
    "email": "contact@acme.com"
  }'
```

```javascript JavaScript
const response = await fetch('https://api.newkipital.com/companies/1', {
  method: 'PUT',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    nombre: 'Acme Corporation Ltd',
    telefono: '+506 2222-4444',
    email: 'contact@acme.com'
  })
});

const company = await response.json();
```
</RequestExample>

<ResponseExample>
```json 200 - Success
{
  "id": 1,
  "nombre": "Acme Corporation Ltd",
  "nombreLegal": "Acme Corporation S.A.",
  "cedula": "3-101-123456",
  "actividadEconomica": "Servicios de consultoría",
  "prefijo": "ACME",
  "idExterno": null,
  "direccionExacta": "Avenida Central, San José, Costa Rica",
  "telefono": "+506 2222-4444",
  "email": "contact@acme.com",
  "codigoPostal": "10101",
  "estado": 1,
  "fechaCreacion": "2024-01-15T10:30:00Z",
  "fechaModificacion": "2024-03-04T15:45:00Z",
  "fechaInactivacion": null,
  "creadoPor": 1,
  "modificadoPor": 5
}
```

```json 400 - Bad Request
{
  "statusCode": 400,
  "message": [
    "email must be an email"
  ],
  "error": "Bad Request"
}
```

```json 401 - Unauthorized
{
  "statusCode": 401,
  "message": "Unauthorized"
}
```

```json 403 - Forbidden
{
  "statusCode": 403,
  "message": "Forbidden resource",
  "error": "Insufficient permissions"
}
```

```json 404 - Not Found
{
  "statusCode": 404,
  "message": "Company with ID 1 not found",
  "error": "Not Found"
}
```

```json 409 - Conflict
{
  "statusCode": 409,
  "message": "Company with cedula '3-101-123456' already exists",
  "error": "Conflict"
}
```
</ResponseExample>